{
  "name": "pkuschronicles",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "process-html": "node --env-file=.env scripts/process-html.mjs",
    "yuque-import": "node --env-file=.env scripts/yuque-import.mjs",
    "r2:sync": "node --env-file=.env scripts/r2-sync.mjs",
    "deploy": "npm run build && node --env-file=.env scripts/r2-deploy.mjs && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "pinyin": "^4.0.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1115.0",
    "@types/node": "^25.9.3"
  }
}
